@import "mixins";

#content p.contracts {
  margin: 0px 0 30px 0;
  font-size: 30px;
  font-weight: 700
}

.top-banner {
  height: 84px;
  line-height: 84px;
  display: inline-block;
  font-size: 45px;
  font-weight: 700;
  margin: 0 auto 30px auto;
  font-family: Arial, Helvetica, sans-serif;

  span {
    float: left;
    margin-left: 2px;
  }

  span.number {
    background: url(<%= asset_path 'number_bg.png' %>) no-repeat scroll 0 0 transparent;
    display:block;
    width:48px;
  }

  span.third {
    float: left;
    margin-right: 10px;
  }
}

#tabs {
  margin-top: 50px;
  height: 36px;
  overflow: hidden;
  li {
    list-style-type: none;
    float: left;
    position: relative;
    z-index: 9;
    a {
      width: 186px;
      height: 33px;
      font-size: 12px;
      display: block;
      line-height: 35px;
      @include border-radius(6px 6px 0 0);
      background: url(<%= asset_path('inactive_tab.png') %>) repeat-x;
      &.active {
        height: 36px;
        color: #000;
        background: white;
        border: 1px solid #ccd2d6;
        border-bottom: none;
        @include box-shadow(0, 0, 4px, #ccc);
      }
    }

    & + li {
      margin-left: 3px;
    }
  }
}

table.contract {
  border-top: solid 1px #e5e8ea;
  border-collapse: collapse;
  margin-left: 20px;
  font-size: 12px;
  text-align: left;
  width: 90%;
  margin-bottom: 6px;

  td {
    padding-bottom: 5px;
    &.value {
      width: 420px;
    }

    &.contract-name {
      font-weight: bold;
    }
  }

  &.first {
    border: none;
  }

  tr {
    &.space {
      height: 15px;
    }

    &.labels td {
      padding-top: 3px;
    }
  }

  .price {
    font-size: 14px;
    font-weight: bold;
  }

  .table-box-left {
    width: 120px;
    text-align: right;
    font-size: 10px;
    line-height: 10px;
    padding: 0 14px 0 5px;
  }

  .comments_count {
    background: url(<%= asset_path 'comment.png' %>) no-repeat left center;
    padding-left: 20px;
    a {
      color: black;
    }
  }

  .event {
    .comments_count {
      margin-left: 109px;
      margin-top: 13px;
    }
  }
}

.other-contracts {
  margin-top: 10px;
  padding-left: 20px;
  font-size: 12px;
  border-top: solid 1px #e5e8ea;
  text-align: left;
  background: #f8fafb;
  height: 35px;
  a {
    display: block;
    line-height: 35px;
    padding-right: 12px;
    float: left;
    border-radius: 0 0 4px 0;
  }
}

.dailyprice {
  margin-bottom: 20px;
  text-align: center;
}

.graph {
  height: 200px;
  margin-bottom: 15px;
}


table.for-graph {
  margin: 0 0 30px 40px;
  font-size: 12px;
  thead {
    font-weight: 700;
  }
  tr {
    height: 30px;
    line-height: 30px;
    border-top: solid 1px #cbd0d4
  }
  &, thead tr {
    border: none !important;
  }
  td {
    width: 100px;
    &.resort {
      padding-left: 5px;
      width: 210px;
    }
  }
}

#search_panel {
  text-align: center;
  position: relative;
  clear: both;
  height: 70px;
  font-size: 12px;
  background-image: url(<%= asset_path 'search_panel_bg.png' %>);
  background-repeat: no-repeat;
  background-position: 480px center;

  a {
    position: absolute;
    top: 28px;
    right: 11%;
  }

  form {
    display: inline-block;
    height: 40px;
    width: 480px;
    margin: 15px 0 0;
  }

  input[type="text"] {
    vertical-align: top;
    margin: 0px;
    width: 360px;
    height: 40px;
    line-height: 40px;
    color: #4c4c4c;
    padding: 0px 20px;
    font-size: 14px;
    @include shaded-input(#ccd2d6);
  }

  input[type="button"] {
    line-height: 1px;
    font-size: 0px;
    color:#1b81d0;
    padding: 0px;
    margin: 1px 0 0 3px;
    width: 60px;
    height: 40px;
    border: none;
    background: url(<%= asset_path 'button_bg.png' %>);
    &:hover {
      background: url(<%= asset_path 'button_bg.png' %>) scroll 0 80px;
    }
    &:active {
      background: url(<%= asset_path 'button_bg.png' %>) scroll 0 40px;
    }
  }
}

#tab_most_commented, #tab_recent_comments {
  display: none;
}

.contracts-footer {
  margin: 30px auto;
}

#slider {
  li {
    list-style-type: none;
    padding-top: 50px;
  }
}

.cta {
  width: 209px;
  margin: 0 auto;
}
